---
import type { Article } from "../../../lib/newt/types";

interface Props {
	metadata: Pick<Article, "title" | "slug" | "description" | "thambnail" | "category" | "tags">;
}

const { metadata } = Astro.props;
---
