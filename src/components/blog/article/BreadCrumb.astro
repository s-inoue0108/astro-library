---
import { svgPaths, svgViewBoxes } from "../../../lib/svg/paths";
import type { Article } from "../../../lib/newt/types";

interface Props {
	metadata: Pick<Article, "title" | "slug" | "category">;
}

const { metadata } = Astro.props;
const { title, slug, category } = metadata;
---
